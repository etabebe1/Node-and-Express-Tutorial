// Bellow is the function to be exported

const greetingFunc = (name) => {
  console.log(`Hello there ${name} this is Jeremiah`);
};

module.exports = greetingFunc;

// The ES6 Module bellow
// export default greetingFunc;
